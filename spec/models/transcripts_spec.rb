require 'spec_helper'

describe Transcripts do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    Transcripts.create!(@valid_attributes)
  end
end
